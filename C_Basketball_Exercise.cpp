#include <bits/stdc++.h>
using namespace std;
#define int long long
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin>>n;
    vector<int> h1(n),h2(n);
    for(int i=0;i<n;i++)
    cin>>h1[i];
    for(int i=0;i<n;i++)
    cin>>h2[i];
    
    

    return 0;
}